.sidebar.pure-u-1.pure-u-md-1-4

    h3
        a(href="/") Blog

    if (user)
        h5#user(data-username="#{user.username}") #{user.username}
        a#logout(href="javascript:;") 登出
        | |
        a(href="/publish") 发布
    else
        a(href="/login") 登录
        | |
        a(href="/register") 注册