.sidebar.pure-u-1.pure-u-md-1-4.clearfix

    h3
        a(href="/") Blog

    if (user)
        .control
            a.J_navToggle 三
        .nav
            a#user(href="#{'/userMessage/' + user._id}" data-userId="#{user._id}" data-username="#{user.username}") #{user.nickname || user.username}
            a(href="/publish") 发布文章
            a(href="#{'/userMessage/' + user._id}" data-username="#{user.username}") 我的消息
            a(href="#{'/userInfo/' + user._id}") 账号设置
            a#logout(href="javascript:;") 退出
    else
        //a(href="/login") 登录
        //a(href="/register") 注册
