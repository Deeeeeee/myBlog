extends layout
block stylesheets
    link(rel="stylesheet", href="/css/index.min.css")
block content
    .pure-g
        include includes/sidebar
        div.main.pure-u-1.pure-u-md-3-4
            ul.article-list
                each item in articles
                    li
                        h5
                            a(href="/article?articleId=#{item._id}") #{item.title}
                        p #{item.username} || #{item.time.minute}
                            if(user && user.username == item.username)
                                |  ||
                                a(href="/publish?articleId=#{item._id}") 编辑
                                |  ||
                                a.J_delArticle(href="javascript:;" data-id="#{item._id}" ) 删除
                        p
                            !{item.content}
block scripts
    script.
        require(['main'], function () {
            require(['page/index']);
        });